 public enum CellType
 { 
    Empty,
    Red,
    Magenta,
    Green,
    Blue
 }

 //Enums are a list of values, each of which is always unique. 
 //You can't have two values in the enum with the same name. 
 //This makes them quite useful for defining all possible value states a function can handle.